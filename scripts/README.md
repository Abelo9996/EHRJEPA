# scripts/

Command-line entry points. Empty placeholder — the planned scripts are
`build_meds.py` (source extract -> MEDS), `build_vocab.py`, `pretrain.py`,
and `evaluate.py`. Each will be a thin argparse wrapper over `ehrjepa.*`;
no logic lives here.
