# Python
__pycache__/
*.py[cod]
*$py.class
*.so
build/
dist/
*.egg-info/
*.egg

# Environments
.venv/
venv/
env/
ENV/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Data: nothing under the top-level data/ is tracked. MIMIC-IV, EHRSHOT and any
# MEDS extract derived from them are credentialed and must never be committed.
# Leading slash so this does not also swallow src/ehrjepa/data/.
/data/
*.parquet
*.csv
*.csv.gz

# ...except the tiny synthetic fixtures the ETL tests run against. These are
# hand-written, contain no real patient data, and must be tracked.
!tests/fixtures/**

# ...and except the metric logs of the recorded experiments, which are derived
# aggregates over a public synthetic dataset and are the point of docs/.
!docs/experiments/**

# The text-init embedding tables (ehrjepa.data.code_text) are a derived artifact
# of a public code table and a public sentence model: ~30 MB per width, rebuilt
# in a couple of minutes by `python -m ehrjepa.data.code_text build`. Their
# coverage JSON is small, is the thing worth reviewing, and is committed under
# docs/experiments/.
code_init_text_*.npy

# Runs, checkpoints, logs
checkpoints/
runs/
logs/
outputs/
results/
*.pth
*.pth.tar
*.ckpt
*.log
*.tmp
